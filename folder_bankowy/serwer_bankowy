def dopisz_do_systemu(linie, nowe_konto):
    with open('folder_bankowy/bank.py',encoding="utf-8",mode='w') as plik:
        plik.write(f' if wyszukaj_konto =={[nowe_konto]}zrób_działanie()')
    plik.writelines(linie)
